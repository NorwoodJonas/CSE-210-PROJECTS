Requirements:
-Make a fraction class
    -That class has "attributes" for the numerator and denominator
    -Has functions to get and change those things
    -Has methods that return both fractional and decimal notation

Fractions:
    numerator
    denominator

    Fraction()
    Fraction(int x) //whole number
    Fraction(int numerator, int denominator)

    GetNumerator()
    GetDenominator()
    SetNumerator()
    SetDenominator()

    GetFractionAsString()
    ToFloat()